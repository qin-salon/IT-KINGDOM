import React from "react";
import Layout from "components/Layout";

const LoginPage = (): JSX.Element => (
  <Layout>
    <h1>ログイン後のページ</h1>
    <p>このページがサロンメンバー専用のページです。まだ仕様が未完成です。</p>
  </Layout>
);

export default LoginPage;
